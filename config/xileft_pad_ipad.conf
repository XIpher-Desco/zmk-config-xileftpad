# xileft pad iPad 版 ― ビルド設定
#
# 通常版 (xileft_pad.conf) との唯一の違いは CONFIG_ZMK_POINTING を
# 有効化していないこと。マウスボタンもホイールも HID に出さない
# 「キーボードだけ」のデバイスになる。
#
# ★ POINTING の有無で HID レポートディスクリプタが変わるため、
#   通常版と iPad 版を同じ BT プロファイルで行き来すると、
#   ホスト側のキャッシュが食い違って誤動作する。
#   別のプロファイル番号を割り当てて運用すること。

# --- ロータリーエンコーダ（EC11 x3）---
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# --- マウス（意図的に無効）---
# CONFIG_ZMK_POINTING=y

# --- LED ステータス表示（zmk-rgbled-widget）---
# 設定の意味と調整方法は config/xileft_pad.conf の同じ節を参照。
CONFIG_RGBLED_WIDGET=y
CONFIG_RGBLED_WIDGET_BATTERY_BLINK_MS=8000
CONFIG_RGBLED_WIDGET_INTERVAL_MS=500
CONFIG_RGBLED_WIDGET_CONN_BLINK_MS=1000

# --- 省電力（必要になったら有効化）---
# CONFIG_ZMK_SLEEP=y
# CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000

# --- デバッグ用 ---
# CONFIG_ZMK_USB_LOGGING=y
